@{
    ViewBag.Title = "_Layout.Basic"; 
}

<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <title>@ViewBag.Title - My ASP.NET MVC Application</title>
        <link href="~/favicon.ico" rel="shortcut icon" type="image/x-icon" />
        <meta name="viewport" content="width=device-width" />
        @Styles.Render("~/Content/css")
        @Scripts.Render("~/bundles/modernizr")
    </head>
    <body>
        <header>
            <div class="content-wrapper">
                <div class="float-left">
                    <p class="site-title">@Html.ActionLink("Let youself explore Europe...", "Index", "Home")</p>
                    <br />
                </div>
                <div class="float-right">
                    <section id="login">
                        @Html.Partial("_LoginPartial")
                    </section>
                </div>
            </div>
        </header>
        <div id="body">
            @RenderSection("featured1", required: false)
            <section class="content-wrapper main-content clear-fix">
                <!--If i want 2 rows of menu items then i should use following code.Just need to change these menu items
                 <div class="content-wrapper">
                    <div class="float-right">
                 <nav>
                        <ul id="container">
                            <li>@Html.ActionLink("Map", "Index", "Map")</li>
                            </ul>
                      <ul id="container2">
                            <li>@Html.ActionLink("About", "About", "Home")</li>
                          </ul>
                     <ul id="container3">
                            <li>@Html.ActionLink("Contact", "Contact", "Home")</li>
                        </ul>
                    </nav>
                    </div>
                    </div>-->
                @RenderBody()
            </section>
        </div>
        <div class="content-wrapper">
                    <div class="float-right">
                 <nav>
                        <ul id="container">
                            <li>@Html.ActionLink("Map", "Index", "Map")</li>
                            </ul>
                      <ul id="container2">
                            <li>@Html.ActionLink("About", "About", "Home")</li>
                          </ul>
                     <ul id="container3">
                            <li>@Html.ActionLink("Contact", "Contact", "Home")</li>
                        </ul>
                    </nav>
                    </div>
                    </div>

        @Scripts.Render("~/bundles/jquery")
        @RenderSection("scripts", required: false)
    </body>
</html>
